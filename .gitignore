# Gradle
.gradle/
build/
out/
src/generated/

# NeoForge dev environment
run/
runs/

# IDE
.idea/
*.iml
.vscode/
.project
.classpath
.settings/

# Local jars dropped into libs/ for inspection or offline use.
# Keeps libs/README.md tracked so the folder's purpose is documented.
libs/*.jar

# PNGs derived from SVGs at build time by the rasterizeIcons Gradle task.
# The SVG masters are the source of truth; the PNGs regenerate on every build.
src/main/resources/icon.png
src/main/resources/icon-transparent.png
src/main/resources/assets/jackittome/textures/gui/jack_button.png

# OS
.DS_Store
Thumbs.db
